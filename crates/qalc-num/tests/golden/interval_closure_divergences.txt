# Rows of interval_closure.txt where this port disagrees with the
# reference, as `key<TAB>diagnosis`. Generated by the C++ shim and
# the worker in tests/interval_closure.rs -- see its module docs.
v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v03 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v03 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v04 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v04 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v05 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v05 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v06 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v06 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v07 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v07 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 * v03	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v04	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v05	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v06	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v07	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v23	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v25	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v26	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v27	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v29	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v30	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 * v39	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v40	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v42	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 * v43	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v12 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 * v03	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v04	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v05	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v06	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v07	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v23	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v25	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v26	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v27	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v29	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v30	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 * v39	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v40	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v42	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 * v43	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v21 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v23 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v23 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v25 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v25 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v26 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v26 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v27 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v27 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v29 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v29 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v30 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v30 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v02	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v08	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v09	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v12	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v13	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v14	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v15	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v16	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v17	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v18	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v21	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v22	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v24	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v36	operand v31/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v37	operand v31/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v38	operand v31/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 * v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v00	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v01	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v02	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v08	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v09	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v12	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v13	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v14	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v15	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v16	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v17	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v18	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v21	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v22	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v24	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v36	operand v32/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v37	operand v32/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 * v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v00	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v02	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v08	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v09	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v12	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v13	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v14	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v15	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v16	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v17	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v18	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v21	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v22	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v24	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v36	operand v33/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v37	operand v33/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 * v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v00	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v01	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v02	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v08	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v09	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v12	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v13	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v14	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v15	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v16	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v17	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v18	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v21	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v22	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v24	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v36	operand v34/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v37	operand v34/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 * v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v00	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v01	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v02	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v08	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v09	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v12	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v13	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v14	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v15	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v16	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v17	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v18	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v21	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v22	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v24	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v36	operand v35/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v37	operand v35/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v38	operand v35/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 * v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v00	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v01	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v02	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v05	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v08	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v09	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v11	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v12	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v13	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v14	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v15	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v16	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v17	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v18	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v20	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v21	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v22	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v23	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v24	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v25	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v37	operand v36/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v38	operand v36/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 * v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v00	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v01	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v02	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v05	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v08	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v09	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v11	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v12	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v13	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v14	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v15	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v16	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v17	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v18	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v20	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v21	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v22	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v23	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v24	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v25	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v36	operand v37/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v38	operand v37/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 * v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v00	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v01	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v03	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v04	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v05	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v06	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v07	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v23	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v25	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v26	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v27	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v28	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v29	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v30	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v31	operand v38/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v35	operand v38/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v36	operand v38/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v37	operand v38/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v39	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v40	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v41	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v42	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 * v43	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v39 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v39 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v40 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v40 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v42 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v42 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v11	* refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v43 * v20	* refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v43 * v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 * v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v00	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v02	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v08	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v09	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v11	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v12	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v13	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v14	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v15	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v16	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v17	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v18	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v20	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v21	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v22	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v24	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v36	operand v31/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v37	operand v31/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v38	operand v31/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 + v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v00	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v02	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v08	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v09	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v11	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v12	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v13	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v14	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v15	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v16	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v17	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v18	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v20	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v21	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v22	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v24	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v36	operand v32/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v37	operand v32/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v38	operand v32/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 + v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v00	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v02	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v08	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v09	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v11	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v12	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v13	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v14	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v15	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v16	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v17	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v18	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v20	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v21	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v22	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v24	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v36	operand v33/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v37	operand v33/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v38	operand v33/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 + v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v01	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v02	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v08	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v09	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v11	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v12	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v13	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v14	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v15	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v16	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v17	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v18	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v20	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v21	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v22	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v24	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v36	operand v34/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v37	operand v34/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v38	operand v34/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 + v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v01	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v02	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v08	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v09	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v11	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v12	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v13	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v14	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v15	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v16	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v17	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v18	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v20	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v21	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v22	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v24	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v36	operand v35/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v37	operand v35/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v38	operand v35/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 + v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v00	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v01	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v02	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v05	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v08	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v09	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v11	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v12	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v13	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v14	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v15	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v16	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v17	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v18	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v20	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v21	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v22	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v23	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v24	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v25	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v37	operand v36/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v38	operand v36/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 + v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v01	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v02	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v05	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v08	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v09	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v11	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v12	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v13	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v14	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v15	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v16	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v17	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v18	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v20	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v21	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v22	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v23	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v24	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v25	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v36	operand v37/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v38	operand v37/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 + v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v01	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v02	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v03	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v04	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v05	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v06	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v07	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v08	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v09	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v10	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v12	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v13	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v14	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v15	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v16	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v17	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v18	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v19	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v21	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v22	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v23	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v24	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v25	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v26	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v27	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v28	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v29	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v30	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v31	operand v38/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v32	operand v38/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v33	operand v38/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v34	operand v38/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v35	operand v38/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v36	operand v38/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v37	operand v38/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v39	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v40	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v41	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v42	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 + v43	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 + v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v01	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v02	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v08	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v09	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v11	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v12	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v13	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v14	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v15	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v16	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v17	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v18	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v20	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v21	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v22	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v24	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v36	operand v31/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v37	operand v31/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v38	operand v31/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 - v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v01	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v02	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v08	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v09	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v11	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v12	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v13	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v14	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v15	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v16	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v17	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v18	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v20	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v21	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v22	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v24	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v36	operand v32/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v37	operand v32/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v38	operand v32/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 - v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v00	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v02	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v08	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v09	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v11	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v12	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v13	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v14	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v15	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v16	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v17	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v18	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v20	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v21	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v22	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v24	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v36	operand v33/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v37	operand v33/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v38	operand v33/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 - v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v00	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v02	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v08	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v09	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v11	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v12	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v13	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v14	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v15	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v16	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v17	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v18	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v20	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v21	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v22	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v24	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v36	operand v34/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v37	operand v34/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v38	operand v34/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 - v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v00	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v02	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v08	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v09	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v11	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v12	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v13	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v14	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v15	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v16	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v17	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v18	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v20	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v21	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v22	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v24	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v36	operand v35/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v37	operand v35/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v38	operand v35/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 - v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v00	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v01	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v02	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v05	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v08	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v09	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v11	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v12	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v13	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v14	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v15	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v16	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v17	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v18	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v20	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v21	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v22	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v23	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v24	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v25	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v37	operand v36/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v38	operand v36/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 - v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v00	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v02	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v05	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v08	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v09	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v11	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v12	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v13	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v14	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v15	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v16	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v17	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v18	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v20	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v21	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v22	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v23	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v24	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v25	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v36	operand v37/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v38	operand v37/v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 - v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v00	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v02	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v03	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v04	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v05	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v06	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v07	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v08	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v09	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v10	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v11	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v12	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v13	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v14	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v15	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v16	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v17	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v18	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v19	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v20	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v21	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v22	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v23	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v24	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v25	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v26	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v27	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v28	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v29	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v30	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v31	operand v38/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v32	operand v38/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v33	operand v38/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v34	operand v38/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v35	operand v38/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v36	operand v38/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v37	operand v38/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v39	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v40	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v41	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v42	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 - v43	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 - v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v03 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v04 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v05 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v06 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v07 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v08 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v09 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 / v41	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out shifted
v10 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v10 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 / v03	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v04	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v05	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v06	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v07	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v23	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v25	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v26	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v27	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v29	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v30	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 / v39	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v40	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v42	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 / v43	/ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v12 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v12 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v13 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v14 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v15 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v16 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v17 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v17 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v18 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v19 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 / v03	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v04	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v05	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v06	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v07	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v23	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v25	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v26	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v27	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v29	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v30	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 / v39	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v40	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v42	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 / v43	/ refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v21 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v21 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v22 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v23 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v24 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v25 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v26 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v27 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v28 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 / v41	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out shifted
v29 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v29 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v30 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v00	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v01	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v09	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v18	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v24	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v36	operand v31/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v37	operand v31/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 / v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v00	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v01	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v09	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v18	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v24	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v36	operand v32/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v37	operand v32/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 / v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v00	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v09	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v18	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v24	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v36	operand v33/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v37	operand v33/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 / v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v00	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v01	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v09	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v18	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v24	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v36	operand v34/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v37	operand v34/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 / v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v00	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v01	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v09	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v18	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v24	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v36	operand v35/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v37	operand v35/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 / v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v00	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v01	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v05	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v09	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v18	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v23	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v24	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v25	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v37	operand v36/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 / v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v00	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v01	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v05	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v09	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v18	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v23	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v24	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v25	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v36	operand v37/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 / v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v03	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v04	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v05	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v06	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v07	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v23	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v25	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v26	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v27	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v28	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v29	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v30	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v36	operand v38/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v37	operand v38/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v39	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v40	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v41	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v42	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 / v43	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v39 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v40 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v41 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 / v41	/ produces an imaginary component the reference does not (rounding dust the reference cancels exactly)
v42 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v42 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v09	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v10	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v18	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v19	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v24	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v28	complex division is computed as z*(1/w) with 1/w = conj(w)/|w|^2, which mentions w's parts more than once and loses the dependency, so the enclosure comes out wider
v43 / v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 / v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v15	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v16	0 raised to an interval exponent that touches zero: the port includes the x^0 = 1 endpoint, the reference stays at 0
v02 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v22	0 raised to an interval exponent that touches zero: the port includes the x^0 = 1 endpoint, the reference stays at 0
v02 ^ v24	^ refuses a zero base with a complex interval exponent; the reference returns 0 because the exponent's real part is known positive
v02 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v29	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v30	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v02 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v03 ^ v01	^ refuses an infinite operand (v01); the reference returns the limit
v03 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v03 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v04 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v04 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v04 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v04 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v04 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v04 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v04 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v04 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v04 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v04 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v09	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v10	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v17	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v18	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v19	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v06 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v28	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v06 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v06 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v06 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v07 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v07 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v07	raising an interval that straddles zero to an even power: the port combines endpoints instead of recognising the result is non-negative, so the enclosure runs below zero
v08 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v08 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v08 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v08 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v08 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v08 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v08 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v09 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v09 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v09 ^ v07	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v09 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v09 ^ v18	^ drops the imaginary component the reference produces, returning a purely real result
v09 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v09 ^ v29	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v09 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v09 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v09 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v10 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v10 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v10 ^ v07	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v10 ^ v21	^ drops the imaginary component the reference produces, returning a purely real result
v10 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v10 ^ v29	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v10 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v10 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v10 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v11 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v11 ^ v07	^ refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v12 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v07	raising an interval that straddles zero to an even power: the port combines endpoints instead of recognising the result is non-negative, so the enclosure runs below zero
v12 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v12 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v12 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v12 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v12 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v12 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v12 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v07	raising an interval that straddles zero to an even power: the port combines endpoints instead of recognising the result is non-negative, so the enclosure runs below zero
v13 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v15	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v13 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v13 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v13 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v13 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v13 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v13 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v29	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v30	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v13 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v07	raising an interval that straddles zero to an even power: the port combines endpoints instead of recognising the result is non-negative, so the enclosure runs below zero
v14 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v15	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v14 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v14 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v14 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v14 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v14 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v14 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v29	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v30	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v14 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v07	raising an interval that straddles zero to an even power: the port combines endpoints instead of recognising the result is non-negative, so the enclosure runs below zero
v15 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v15 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v15 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v15 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v15 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v15 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v15 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v16 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v16 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v16 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v16 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v17 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v17 ^ v07	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v17 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v17 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v18 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v18 ^ v12	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v13	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v14	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v18 ^ v18	^ drops the imaginary component the reference produces, returning a purely real result
v18 ^ v22	^ drops the imaginary component the reference produces, returning a purely real result
v18 ^ v23	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v25	^ drops the imaginary component the reference produces, returning a purely real result
v18 ^ v26	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v28	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v29	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v18 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v19 ^ v12	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v13	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v14	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v21	^ drops the imaginary component the reference produces, returning a purely real result
v19 ^ v22	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v19 ^ v23	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v19 ^ v26	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v19 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v19 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v20 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v15	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v21 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v21 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v21 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v21 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v21 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v21 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v22 ^ v02	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v04	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v08	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v12	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v13	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v14	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v18	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v22 ^ v19	^ refuses a complex interval exponent; the reference evaluates exp(y*ln x) over the box
v22 ^ v21	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v27	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v22 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 ^ v40	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v22 ^ v43	^ answers an indeterminate power (0 or infinity to an exponent that is not known non-zero); the reference returns false and leaves the value untouched
v23 ^ v10	^ drops the imaginary component the reference produces, returning a purely real result
v23 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v19	^ drops the imaginary component the reference produces, returning a purely real result
v23 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v23 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v03	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v24 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v24 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v22	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v25	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v24 ^ v29	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v24 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v24 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 ^ v39	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v24 ^ v42	^ drops the imaginary component the reference produces, returning a purely real result
v24 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v25 ^ v09	^ drops the imaginary component the reference produces, returning a purely real result
v25 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v18	^ drops the imaginary component the reference produces, returning a purely real result
v25 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v25 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v28	^ drops the imaginary component the reference produces, returning a purely real result
v25 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v25 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 ^ v41	^ drops the imaginary component the reference produces, returning a purely real result
v26 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v26 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v26 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v27 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v18	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v27 ^ v19	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v27 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v27 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v27 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v27 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v27 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 ^ v41	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v04	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v06	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v28 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v28 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v29	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 ^ v40	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v28 ^ v43	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v29 ^ v18	^ drops the imaginary component the reference produces, returning a purely real result
v29 ^ v19	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v29 ^ v24	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v29 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v29 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v29 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v29 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v29 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 ^ v41	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v30 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v30 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v30 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v30 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v30 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v30 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v30 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v30 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v30 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v30 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v01	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v02	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v08	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v09	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v12	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v13	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v14	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v15	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v16	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v17	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v18	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v21	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v22	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v24	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v36	operand v31/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v37	operand v31/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 ^ v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v01	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v02	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v08	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v09	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v12	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v13	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v14	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v15	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v16	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v17	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v18	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v21	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v22	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v24	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v36	operand v32/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v37	operand v32/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 ^ v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v02	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v08	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v09	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v12	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v13	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v14	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v15	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v16	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v17	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v18	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v21	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v22	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v24	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v36	operand v33/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v37	operand v33/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 ^ v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v01	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v02	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v08	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v09	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v12	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v13	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v14	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v15	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v16	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v17	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v18	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v21	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v22	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v24	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v36	operand v34/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v37	operand v34/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 ^ v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v01	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v02	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v08	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v09	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v12	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v13	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v14	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v15	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v16	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v17	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v18	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v21	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v22	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v24	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v36	operand v35/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v37	operand v35/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 ^ v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v02	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v05	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v08	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v09	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v12	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v13	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v14	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v15	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v16	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v17	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v18	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v21	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v22	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v23	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v24	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v25	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v37	operand v36/v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 ^ v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v05	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v08	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v09	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v12	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v13	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v14	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v15	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v16	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v17	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v18	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v21	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v22	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v23	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v24	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v25	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v36	operand v37/v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 ^ v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 ^ v02	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 ^ v05	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v01	^ refuses an infinite operand (v01); the reference returns the limit
v39 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v29	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v30	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v39 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v40 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v40 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v40 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v40 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v40 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v40 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v40 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v40 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v40 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v40 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v41 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v15	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v42 ^ v22	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v42 ^ v24	^ drops the imaginary component the reference produces, returning a purely real result
v42 ^ v25	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v42 ^ v26	^ never returns: the interval branch calls astro-float's pow on endpoints whose result is exactly representable (dyadic base, integral exponent) and its Ziv refinement does not settle
v42 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v08	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v43 ^ v16	^ drops the imaginary component the reference produces, returning a purely real result
v43 ^ v21	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v43 ^ v23	^ drops the imaginary component the reference produces, returning a purely real result
v43 ^ v24	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v43 ^ v25	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v43 ^ v26	^ drops the imaginary component the reference produces, returning a purely real result
v43 ^ v27	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
v43 ^ v29	^ drops the imaginary component the reference produces, returning a purely real result
v43 ^ v30	^ on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v43 ^ v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 ^ v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v06	log refuses an infinite operand (v00); the reference returns the limit
v00 log v10	log refuses an infinite operand (v00); the reference returns the limit
v00 log v19	log refuses an infinite operand (v00); the reference returns the limit
v00 log v28	log refuses an infinite operand (v00); the reference returns the limit
v00 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 log v41	log refuses an infinite operand (v00); the reference returns the limit
v01 log v03	log refuses an infinite operand (v01); the reference returns the limit
v01 log v04	log refuses an infinite operand (v01); the reference returns the limit
v01 log v06	log refuses an infinite operand (v01); the reference returns the limit
v01 log v07	log refuses an infinite operand (v01); the reference returns the limit
v01 log v10	log refuses an infinite operand (v01); the reference returns the limit
v01 log v19	log refuses an infinite operand (v01); the reference returns the limit
v01 log v26	log refuses an infinite operand (v01); the reference returns the limit
v01 log v28	log refuses an infinite operand (v01); the reference returns the limit
v01 log v30	log refuses an infinite operand (v01); the reference returns the limit
v01 log v39	log refuses an infinite operand (v01); the reference returns the limit
v01 log v40	log refuses an infinite operand (v01); the reference returns the limit
v01 log v41	log refuses an infinite operand (v01); the reference returns the limit
v01 log v42	log refuses an infinite operand (v01); the reference returns the limit
v01 log v43	log refuses an infinite operand (v01); the reference returns the limit
v02 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v02 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v02 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v02 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v02 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v00	log refuses an infinite operand (v00); the reference returns the limit
v03 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v03	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v03 log v07	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v03 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v03 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v03 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v03 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v03 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 log v39	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v03 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v00	log refuses an infinite operand (v00); the reference returns the limit
v04 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v04	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v04 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v04 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v04 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v04 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v04 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 log v40	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v04 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v00	log refuses an infinite operand (v00); the reference returns the limit
v05 log v01	log refuses an infinite operand (v01); the reference returns the limit
v05 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v08	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v11	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v05 log v12	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v20	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v05 log v21	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v05 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v00	log refuses an infinite operand (v00); the reference returns the limit
v06 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v06	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v06 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v06 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v06 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v06 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v06 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v00	log refuses an infinite operand (v00); the reference returns the limit
v07 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v07	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v07 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v07 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v07 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v07 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v07 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v08 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v00	log refuses an infinite operand (v00); the reference returns the limit
v09 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v09 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v09 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v09 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v09 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v09 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v00	log refuses an infinite operand (v00); the reference returns the limit
v10 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v10 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v10 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v10 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v10 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v10 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v11 log v03	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v06	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v07	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v10	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v19	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v26	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v28	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 log v39	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v41	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 log v42	log refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v12 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v12 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v13 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v14 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v15 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v16 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v17 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v00	log refuses an infinite operand (v00); the reference returns the limit
v18 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v18 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v18 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v18 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v18 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v18 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v00	log refuses an infinite operand (v00); the reference returns the limit
v19 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v19 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v19 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v19 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v19 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v19 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v20 log v03	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v06	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v07	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v10	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v19	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v26	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v28	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v20 log v39	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v41	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 log v42	log refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v21 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v21 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v03	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v04	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v06	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v07	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v26	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v30	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v39	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v40	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v42	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v22 log v43	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v00	log refuses an infinite operand (v00); the reference returns the limit
v23 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v23 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v23 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v23 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v23 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v00	log refuses an infinite operand (v00); the reference returns the limit
v24 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v24 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v24 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v24 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v24 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v24 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v00	log refuses an infinite operand (v00); the reference returns the limit
v25 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v25 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v25 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v25 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v25 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v00	log refuses an infinite operand (v00); the reference returns the limit
v26 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v26 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v26 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v26 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v26 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v00	log refuses an infinite operand (v00); the reference returns the limit
v27 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v04	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v27 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v27 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v27 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v27 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v27 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 log v40	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out shifted
v27 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v00	log refuses an infinite operand (v00); the reference returns the limit
v28 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v28 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v28 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v28 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v28 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v28 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v00	log refuses an infinite operand (v00); the reference returns the limit
v29 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v29 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v29 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v29 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v29 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v00	log refuses an infinite operand (v00); the reference returns the limit
v30 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v30 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v30 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v30 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v30 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v31 log v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v10	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v19	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v28	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v41	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 log v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v10	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v19	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v28	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v41	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 log v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v10	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v19	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v28	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v41	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 log v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v10	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v19	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v28	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v41	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 log v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v10	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v19	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v28	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v41	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 log v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v03	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v04	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v06	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v07	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v10	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v19	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v26	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v27	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v28	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v29	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v30	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v31	operand v36/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v32	operand v36/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v33	operand v36/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v34	operand v36/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v35	operand v36/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v39	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v40	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v41	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v42	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 log v43	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v03	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v04	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v06	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v07	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v10	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v19	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v26	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v27	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v28	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v29	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v30	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v31	operand v37/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v32	operand v37/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v33	operand v37/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v34	operand v37/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v35	operand v37/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v39	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v40	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v41	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v42	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 log v43	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v03	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v06	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v07	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v10	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v19	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v26	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v28	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v31	operand v38/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v32	operand v38/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v33	operand v38/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v34	operand v38/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v35	operand v38/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v39	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v41	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 log v42	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v00	log refuses an infinite operand (v00); the reference returns the limit
v39 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v03	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v39 log v07	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v39 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v39 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v39 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v39 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v39 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 log v39	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v39 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v00	log refuses an infinite operand (v00); the reference returns the limit
v40 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v04	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v40 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v40 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v40 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v40 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v40 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 log v40	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v40 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v00	log refuses an infinite operand (v00); the reference returns the limit
v41 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v41 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v41 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v41 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v41 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v41 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v00	log refuses an infinite operand (v00); the reference returns the limit
v42 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v42 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v42 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v42 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v42 log v42	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v43 log v00	log refuses an infinite operand (v00); the reference returns the limit
v43 log v02	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v09	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v10	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v16	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v18	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v19	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v27	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v43 log v28	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v29	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v43 log v30	log on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
v43 log v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 log v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 log v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 log v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 log v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 log v41	log refuses a base that is zero, negative or an interval containing zero; the reference divides ln(x) by ln(base) and carries ln(0)'s infinite endpoint through
v43 log v43	log of a value to itself: the reference recognises x == base and returns an exact 1, the port divides ln(x) by ln(base) in floats and keeps a hair of interval width
v00 atan2 v00	atan2 refuses an infinite operand (v00); the reference returns the limit
v00 atan2 v01	atan2 refuses an infinite operand (v00/v01); the reference returns the limit
v00 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v00 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 atan2 v00	atan2 refuses an infinite operand (v01/v00); the reference returns the limit
v01 atan2 v01	atan2 refuses an infinite operand (v01); the reference returns the limit
v01 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v02 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v03 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v04 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v05 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v06 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v07 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v08 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v08 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v12 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v12 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v13 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v13 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v14 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v14 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v15 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v15 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v16 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v16 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v01	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v04	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v06	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v27	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v29	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v30	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v21 atan2 v40	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v21 atan2 v43	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v22 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v22 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v23 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v25 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v26 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 atan2 v08	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v27 atan2 v12	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v27 atan2 v13	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v27 atan2 v14	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v27 atan2 v15	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v27 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v27 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 atan2 v08	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v29 atan2 v12	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v29 atan2 v13	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v29 atan2 v14	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v29 atan2 v15	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v29 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v29 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 atan2 v08	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v30 atan2 v12	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v30 atan2 v13	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v30 atan2 v14	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v30 atan2 v15	atan2 stays on one branch when y straddles zero and x may be negative, so the result runs past +pi instead of widening to the full [-pi, pi]
v30 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v30 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v00	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v01	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v03	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v04	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v05	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v06	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v07	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v08	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v12	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v13	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v14	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v15	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v16	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v21	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v22	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v23	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v25	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v26	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v27	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v29	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v30	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v32	operand v31/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v33	operand v31/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v34	operand v31/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v35	operand v31/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v39	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v40	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v42	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 atan2 v43	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v00	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v01	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v02	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v03	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v04	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v05	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v06	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v07	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v08	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v12	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v13	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v14	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v15	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v16	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v21	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v22	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v23	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v25	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v26	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v27	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v29	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v30	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v31	operand v32/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v33	operand v32/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v34	operand v32/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v35	operand v32/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v39	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v40	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v42	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 atan2 v43	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v00	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v01	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v02	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v03	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v04	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v05	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v06	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v07	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v08	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v12	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v13	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v14	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v15	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v16	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v21	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v22	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v23	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v25	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v26	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v27	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v29	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v30	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v31	operand v33/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v32	operand v33/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v34	operand v33/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v35	operand v33/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v39	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v40	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v42	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 atan2 v43	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v00	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v01	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v02	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v03	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v04	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v05	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v06	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v07	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v08	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v12	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v13	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v14	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v15	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v16	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v21	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v22	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v23	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v25	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v26	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v27	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v29	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v30	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v31	operand v34/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v32	operand v34/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v33	operand v34/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v35	operand v34/v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v39	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v40	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v42	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 atan2 v43	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v00	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v01	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v02	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v03	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v04	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v05	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v06	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v07	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v08	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v12	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v13	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v14	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v15	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v16	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v21	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v22	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v23	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v25	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v26	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v27	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v29	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v30	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v31	operand v35/v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v32	operand v35/v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v33	operand v35/v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v34	operand v35/v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v39	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v40	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v42	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 atan2 v43	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v39 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v40 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 atan2 v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 atan2 v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 atan2 v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 atan2 v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v43 atan2 v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v01 root 3	the reference segfaults: Number::root falls through to its mpfr branch with -infinity's uninitialised fl_value. The port answers
v01 root 5	the reference segfaults: Number::root falls through to its mpfr branch with -infinity's uninitialised fl_value. The port answers
v01 root 7	the reference segfaults: Number::root falls through to its mpfr branch with -infinity's uninitialised fl_value. The port answers
v01 root 9	the reference segfaults: Number::root falls through to its mpfr branch with -infinity's uninitialised fl_value. The port answers
v08 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v08 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v08 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v08 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v12 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v12 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v12 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v12 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v13 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v13 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v13 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v13 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v14 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v14 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v14 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v14 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v15 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v15 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v15 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v15 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v21 root 3	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v21 root 5	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v21 root 7	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v21 root 9	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
v31 root 3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 root 5	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 root 7	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 root 9	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 root 3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 root 5	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 root 7	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 root 9	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 root 3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 root 5	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 root 7	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 root 9	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 root 3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 root 5	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 root 7	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 root 9	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 4	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 5	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 6	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 7	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 8	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 root 9	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i -3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i -2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i -1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i 0	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i 1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i 2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 +i 3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i -3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i -2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i -1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i 0	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i 1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i 2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 +i 3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i -3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i -2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i -1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i 0	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i 1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i 2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 +i 3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i -3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i -2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i -1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i 0	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i 1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i 2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 +i 3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i -3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i -2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i -1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i 0	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i 1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i 2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 +i 3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i -3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i -2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i -1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i 0	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i 1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i 2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 +i 3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i -3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i -2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i -1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i 0	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i 1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i 2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 +i 3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i -3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i -2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i -1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i 0	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i 1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i 2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 +i 3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i -3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i -2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i -1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i 0	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i 1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i 2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 -i 3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i -3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i -2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i -1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i 0	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i 1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i 2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 -i 3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i -3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i -2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i -1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i 0	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i 1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i 2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 -i 3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i -3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i -2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i -1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i 0	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i 1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i 2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 -i 3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i -3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i -2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i -1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i 0	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i 1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i 2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 -i 3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i -3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i -2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i -1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i 0	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i 1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i 2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 -i 3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i -3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i -2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i -1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i 0	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i 1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i 2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 -i 3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i -3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i -2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i -1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i 0	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i 1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i 2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 -i 3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 *i -3	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 *i -2	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 *i -1	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 *i 1	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 *i 2	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 *i 3	*i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v20 *i -3	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 *i -2	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 *i -1	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 *i 1	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 *i 2	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 *i 3	*i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v31 *i -3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i -2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i -1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i 0	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i 1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i 2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 *i 3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i -3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i -2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i -1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i 0	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i 1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i 2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 *i 3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i -3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i -2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i -1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i 0	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i 1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i 2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 *i 3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i -3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i -2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i -1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i 0	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i 1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i 2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 *i 3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i -3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i -2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i -1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i 0	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i 1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i 2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 *i 3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i -3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i -2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i -1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i 0	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i 1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i 2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 *i 3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i -3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i -2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i -1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i 0	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i 1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i 2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 *i 3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i -3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i -2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i -1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i 1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i 2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 *i 3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v11 /i -3	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 /i -2	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 /i -1	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 /i 1	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 /i 2	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v11 /i 3	/i refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
v20 /i -3	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 /i -2	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 /i -1	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 /i 1	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 /i 2	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v20 /i 3	/i refuses an operand with an infinite imaginary part (v20); the reference carries the infinity through componentwise
v31 /i -3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 /i -2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 /i -1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 /i 1	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 /i 2	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v31 /i 3	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i -3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i -2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i -1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i 1	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i 2	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v32 /i 3	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i -3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i -2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i -1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i 1	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i 2	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v33 /i 3	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i -3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i -2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i -1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i 1	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i 2	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v34 /i 3	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i -3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i -2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i -1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i 1	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i 2	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v35 /i 3	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i -3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i -2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i -1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i 1	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i 2	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v36 /i 3	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i -3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i -2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i -1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i 1	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i 2	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v37 /i 3	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i -3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i -2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i -1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i 1	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i 2	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v38 /i 3	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
v42 /i -3	/i leaves a hair of interval width where the reference's result is exact (the port forms the reciprocal interval first and multiplies; the reference applies the exact rational in one rounded operation)
v42 /i 3	/i leaves a hair of interval width where the reference's result is exact (the port forms the reciprocal interval first and multiplies; the reference applies the exact rational in one rounded operation)
v43 /i -3	/i leaves a hair of interval width where the reference's result is exact (the port forms the reciprocal interval first and multiplies; the reference applies the exact rational in one rounded operation)
v43 /i 3	/i leaves a hair of interval width where the reference's result is exact (the port forms the reciprocal interval first and multiplies; the reference applies the exact rational in one rounded operation)
recip v09	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v10	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v18	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v19	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v24	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v28	recip evaluates conj(z)/|z|^2 in interval arithmetic, mentioning z's parts more than once, so the enclosure comes out wider
recip v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
recip v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
neg v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
abs v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v09	sq on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
sq v10	sq on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
sq v11	sq refuses an operand with an infinite imaginary part (v11); the reference carries the infinity through componentwise
sq v17	sq on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
sq v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sq v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v01	sqrt refuses an infinite operand (v01); the reference returns the limit
sqrt v08	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v09	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v10	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v12	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v13	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v14	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v15	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v18	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v19	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v21	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v24	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v28	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
sqrt v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sqrt v41	sqrt refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
cbrt v08	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v09	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v10	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v12	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v13	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v14	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v15	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v18	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v19	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v21	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v24	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v28	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
cbrt v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cbrt v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cbrt v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cbrt v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cbrt v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cbrt v41	odd-degree root refuses a negative or zero-straddling interval (root_i requires a non-negative argument); the reference takes the real odd root of each endpoint
sin v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sin v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asin v09	asin on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
asin v10	asin on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
asin v15	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v17	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v18	asin on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
asin v19	asin on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
asin v22	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v24	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v25	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v28	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v29	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v30	asin refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asin v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v18	sinh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
sinh v19	sinh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
sinh v24	sinh drops the imaginary component the reference produces, returning a purely real result
sinh v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
sinh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v09	asinh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asinh v10	asinh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
asinh v17	asinh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asinh v18	asinh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asinh v19	asinh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
asinh v24	asinh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asinh v28	asinh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
asinh v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
asinh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cos v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acos v07	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v09	acos on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acos v10	acos on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acos v13	acos returns a narrower enclosure than the reference (the reference's endpoints carry rounding dust the port cancels)
acos v14	acos returns a narrower enclosure than the reference (the reference's endpoints carry rounding dust the port cancels)
acos v15	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v17	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v18	acos on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acos v19	acos on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
acos v22	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v23	acos returns a narrower enclosure than the reference (the reference's endpoints carry rounding dust the port cancels)
acos v24	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v25	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v26	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v28	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v29	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v30	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acos v42	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acos v43	acos refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
cosh v18	cosh drops the imaginary component the reference produces, returning a purely real result
cosh v24	cosh drops the imaginary component the reference produces, returning a purely real result
cosh v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
cosh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acosh v08	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acosh v09	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acosh v10	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out disjoint
acosh v12	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v13	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v14	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v15	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v16	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acosh v17	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v18	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acosh v19	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out disjoint
acosh v21	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
acosh v22	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v23	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v24	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v27	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v28	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v29	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v30	acosh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
acosh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acosh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acosh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
acosh v41	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out disjoint
acosh v43	acosh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out disjoint
tan v09	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v10	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v17	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v18	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v19	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v24	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v28	tan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tan v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tan v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v19	atan on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
atan v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atan v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v09	tanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tanh v10	tanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tanh v18	tanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
tanh v19	tanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
tanh v28	tanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
tanh v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
tanh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v00	atanh refuses an infinite operand (v00); the reference returns the limit
atanh v01	atanh refuses an infinite operand (v01); the reference returns the limit
atanh v15	atanh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
atanh v18	atanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
atanh v19	atanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out narrower
atanh v22	atanh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
atanh v24	atanh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
atanh v25	atanh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
atanh v26	atanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
atanh v29	atanh refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
atanh v30	atanh on a complex interval: the port composes real-interval operations that mention each part more than once, so the enclosure comes out wider
atanh v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
atanh v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v01	ln refuses an infinite operand (v01); the reference returns the limit
ln v08	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v12	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v13	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v14	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v15	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v16	ln refuses an interval whose image is unbounded; the reference returns a half-infinite interval
ln v17	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v21	ln refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
ln v22	ln refuses an interval whose image is unbounded; the reference returns a half-infinite interval
ln v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
ln v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
gamma v25	gamma is evaluated at the interval endpoints only, so it misses the interior minimum near 1.4616 and returns too narrow a range
besselj v00	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v01	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v02	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v03	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v04	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v05	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v06	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v07	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v08	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v12	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v13	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v14	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v15	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v16	the reference never returns (mpfr_jn/mpfr_yn on a wide interval); the port refuses because besselj/bessely are unimplemented
besselj v21	the reference never returns (mpfr_jn/mpfr_yn on a wide interval); the port refuses because besselj/bessely are unimplemented
besselj v22	the reference never returns (mpfr_jn/mpfr_yn on a wide interval); the port refuses because besselj/bessely are unimplemented
besselj v23	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v25	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v26	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v27	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v29	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v30	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
besselj v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
besselj v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
besselj v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
besselj v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
besselj v39	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v40	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v42	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
besselj v43	besselj is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v00	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v03	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v05	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v07	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v16	the reference never returns (mpfr_jn/mpfr_yn on a wide interval); the port refuses because besselj/bessely are unimplemented
bessely v22	the reference never returns (mpfr_jn/mpfr_yn on a wide interval); the port refuses because besselj/bessely are unimplemented
bessely v23	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v25	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v26	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
bessely v39	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
bessely v42	bessely is an unimplemented stub that returns false; the reference computes it with mpfr_jn/mpfr_yn
erf v09	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v10	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v17	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v18	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v19	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v24	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v28	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erf v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erf v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erf v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erf v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erf v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erf v41	erf refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v09	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v10	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v17	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v18	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v19	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v24	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v28	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
erfc v31	operand v31 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erfc v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erfc v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erfc v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erfc v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
erfc v41	erfc refuses an interval argument that leaves the real line; the reference continues into the complex plane and returns a complex interval
arg v32	operand v32 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v33	operand v33 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v34	operand v34 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v35	operand v35 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v36	operand v36 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v37	operand v37 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
arg v38	operand v38 was never built: Number::set_interval returns false for an infinite endpoint (is_real() excludes +/-infinity), so test.cc's half-infinite intervals leave v30's [-3:-2] in place
